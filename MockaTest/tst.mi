MODULE tst;
IMPORT SYSTEM;
VAR a:LONGINT; adr:SYSTEM.ADDRESS; c:CARDINAL; 
BEGIN
 a:=LONGCARD(BITSET(a+1)*BITSET(0FFFFFFFEH)); 
 a:=LONGINT(BITSET(a)*BITSET(0FFFFFFFEH));     
 c:=CARDINAL(adr); 
END tst.
