(* Fehlerhafte Strings      *)
(* (MsgStringNotTerminated) *)

"Ein ungeschlossener String,
da er ueber zwei Zeilen geht"

"dieser string endet mit falschem zeichen'
'dito, nur andersherum"

'im string hab' ich das falsche zeichen benutzt'

"und dieser string hoert ueberhaupt nicht mehr auf
